{
  "name": "react-template",
  "version": "1.0.0",
  "description": "A template project for a react app",
  "main": "src/entry.js",
  "scripts": {
    "create": "yarn && cd client && yarn",
    "client:build": "cd client && yarn build",
    "client:build:prod": "cd client && yarn build:prod",
    "client:build:watch": "cd client && yarn build:watch",
    "client:test": "cd client && yarn test",
    "client:test:coverage": "cd client && yarn test:coverage",
    "client:test:update": "cd client && yarn test:update",
    "server:start": "node ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Landell <kyle.landell@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.2",
    "compression": "^1.6.2",
    "dotenv": "^4.0.0",
    "express": "^4.15.3",
    "morgan": "^1.8.1",
    "yes-https": "0.0.4"
  },
  "devDependencies": {}
}
